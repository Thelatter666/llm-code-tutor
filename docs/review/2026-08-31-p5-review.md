# P5 审阅与裁定（第三任总指挥）

- 审阅日期：2026-08-31
- 审阅对象：`feat/p5-exercise-mistakebook` @ `da5ba8c`（Task 17 完成报告交付点）
- 审阅依据：第一任交接 §8 方法论 + 第二任交接 §4 验证欠账清单 + P5 计划文档
- 审阅方式：全部独立复跑与实拆，**不采信执行 agent 自证数字**；变异测试均为
  「改坏 → 跑目标测试必须失败 → 字节级还原 → `git status` 干净」
- **结论：验收通过。无阻塞项、无高危项；1 项中危（完成报告变异记录用例名失实，属报告准确性问题，不涉代码缺陷）。**

---

## 1. 独立复跑结果（全部本次实跑）

| 门槛 | 报告声称 | 本任独立实测 | 判定 |
|---|---|---|---|
| 后端 pytest（第 1 次） | 955 passed / 2 skipped，181.94s | **955 passed / 2 skipped，172.70s** | ✅ 数字吻合 |
| 后端 pytest（还原后第 2 次） | — | **955 passed / 2 skipped，173.37s** | ✅ 变异还原无残留 |
| 全树收集 | 957 collected | **957 collected**（= 955 + 2） | ✅ |
| 2 条 skip | `test_retrieval_quality_realistic.py` 语料条件 skip | `test_retrieval_quality_realistic.py:286/308`（「只有长切片语料才会出现这种稀释」），与 P5 无关 | ✅ |
| 前端 vitest | 8 passed | **8 passed** | ✅ |
| `vue-tsc --noEmit` | 零错误 | **exit 0，零输出** | ✅ |
| `vite build` | 通过（1m07s） | **通过（33.45s）** | ✅ 耗时差异属机器状态，产物吻合 |
| chunk 尺寸 | Exercise 14.62 kB / Mistake 7.86 kB | **ExerciseView-t_tmdNSN.js 14.62 kB（CSS 4.17）· MistakeBookView-B9NNSUOq.js 7.86 kB（CSS 2.99）**，均独立懒加载 chunk | ✅ 逐字节吻合 |
| lint | 35 errors（22 fixable） | **Found 35 errors / 22 fixable**，未增长 | ✅ 与 M-1 存量基线一致 |
| H-1 负载敏感 | 隔离复跑 5/5 | 本任两次全量（无并行负载）均通过；未与前端构建并行 | ✅ 口径一致 |

基线链条核算：开工前基线 661（第二任独立复跑）→ Task 0–8 新增 101（P5 阶段交接 §2 清单逐文件合计 101）→ 762 → Task 9–17 新增 193 → 955。链条自洽。

## 2. 变异测试逐条实拆（8 + 1 条）

计划 Task 17 列 7 条（第 7 条含两个方向），执行 agent 报告实跑 8 条。本任全部独立重做：

| # | 突变 | 实拆位置 | 击杀用例（本任实测） | 判定 |
|---|---|---|---|---|
| 1 | `mastery.py` 错误分支删 `mastered=False` 回滚 | `apply_answer` 错误分支 | `test_mastery.py::test_mastered_rolls_back_on_wrong` + `test_mistake_service.py::test_record_result_full_cycle_with_rollback`（**2 failed**） | ✅ 与报告一致 |
| 2 | `judging.py` 删 multi 真子集 50 分分支 | `judge_multi` | `test_judging.py::test_multi_subset_scores_50_and_is_wrong` + `test_multi_detail_reports_missing_and_wrong` + `test_exercise_service.py::test_multi_subset_scores_50_and_is_wrong`（**3 failed**） | ✅ 一致 |
| 3 | `exercise_service.py` 删 15s 预算时间比较 | `_judge_coding` 循环头 | `test_exercise_service.py::test_coding_budget_abort_marks_skipped_and_scores_by_ratio`（**1 failed**，`budget_exceeded False is True`） | ✅ 一致 |
| 4 | `mistake_service.py` RandomFill 误标 profile | `recommendations` 补足分支 | `test_recommendations_random_fill_is_marked` / `test_recommendations_without_mistakes_all_random` / `test_recommendations_marks_profile_and_random` / `test_recommendations_default_limit_is_five`（**4 failed**） | ✅ 一致 |
| 5 | `exercise_service.py` hint intent 强制按 seek_answer | `stream_hint` `mode = resolve_mode(...)` | **实际击杀者：`test_audit_mode_is_null_for_the_exempt_intent`（1 failed）**。报告所称 `test_review_my_code_is_exempt_from_mode_but_not_from_floor` 对该突变**不敏感** | ⚠️ 见 §3 中危 M-5R |
| 5b（本任补充） | 装配层 `intent=intent` 强制改 `"seek_answer"` | `stream_hint` `self._prompts.assemble(...)` | `test_review_my_code_is_exempt_from_mode_but_not_from_floor`（**1 failed**，system prompt 出现 strict 档位而非豁免标记） | ✅ 豁免契约真实有守 |
| 6 | `seeds/exercises.py` 删主键存在性检查 | `seed_exercises` | `test_seed_exercises_is_idempotent` + `test_seed_exercises_does_not_overwrite_edits`（**2 failed**） | ✅ 一致 |
| 7 | `reset_mastered` 保留 `consecutive_correct` | `reset_mastered` | `test_reset_mastered_clears_streak_keeps_history` + `test_reset_mastered_returns_reset_state` + `test_reset_of_unmastered_entry_starts_a_new_round`（**3 failed**；报告称 2） | ✅ 裁定 2 被守住；多杀 1 条非问题 |
| 8 | 学生端列表去 published 过滤 | `list_exercises` | `test_exercise_api.py::test_list_exposes_only_published`（**1 failed**）；submit 路径 draft 4040 由 `get_published` 独立守住，未受该突变影响 | ✅ 与报告的诚实记录一致 |

8 处突变全部被真失败击杀，全部字节级还原，结束后 `git status` 干净、全量 955/2 复跑通过。

## 3. 裁定分级

### 阻塞（不合并）

无。

### 高危（同批处理）

无。

### 中危（报告更正，不涉代码；M-5R）

**M-5R：完成报告 §4 变异记录 M5 的击杀用例名与实测不符。**
- 报告声称 M5（`mode = resolve_mode(...)` 强制 seek_answer）被 `test_review_my_code_is_exempt_from_mode_but_not_from_floor` 击杀。
- 实测该突变只影响**审计记录的 `mode` 字段**（`stream_hint` 中 `mode` 局部变量不参与 prompt 装配——装配层靠 `intent=intent` 独立解析豁免），实际击杀者是 `test_audit_mode_is_null_for_the_exempt_intent`。
- **功能无缺陷**：豁免语义在装配层另有 `intent=intent` 这条真防线，本任补充变异 5b 证实其真实可被击杀。两条路径（prompt 豁免 / 审计 mode）各有专项用例，覆盖是完整的。
- 处置：完成报告的 M5 行按本文 §2 更正即可；agent 无需返工代码。教训重申第一任交接 §10：**agent 报告里的「实测」清单也要抽验**——本次 8 条里抽出 1 条即发现记录失实。

### 低危（归档即可）

- **L-1**：M7（reset 保留连对）报告称 2 failed，实测 3 failed（多出 `test_reset_of_unmastered_entry_starts_a_new_round`）。多杀非问题，属报告数字未更新。
- **L-2**：`budget_limit_s` 回传 `15.0`（float），计划契约定稿 8 写 `15`（int）。spec 未声明类型，前端仅做展示，无行为影响。归档不改。

## 4. spec 漂移核查（计划 §5 的 11 条回写）

逐条对 spec 核实，全部落档且标注（P5 补记）：

| # | 回写项 | spec 位置 | 实测 |
|---|---|---|---|
| 1 | admin 习题 CRUD 五端点 | §6.2 admin·exercise 行 + 端点补记 | ✅ 行 318/346-349 |
| 2 | hint 请求体 `{intent, answer?}` | §6.2 exercise 行 + 补记 | ✅ 行 316/336-340 |
| 3 | hint done 九字段 | §6.1 附注 | ✅ 行 297-301 |
| 4 | `DELETE /mistakes/{id}/mastered` 语义（清零连对） | §8.5 P5 补记 | ✅ 行 573-579 |
| 5 | 简答评分 prompt 形态 / Mock 启发式 / 5021 不落库 | §5.1 P5 补记 | ✅ 行 256-269 |
| 6 | `backend/seeds/` 落地（uuid5 幂等） | §4.3 + §11 落地实况 | ✅ 行 175/696-702 |
| 7 | answer / test_cases 各题型 JSON 形态 | §5 P5 补记（含 judge_detail 表） | ✅ 行 216-235 |
| 8 | `GET /exercises` 分页 + facets 超集 | §6.2 端点补记 | ✅ 行 330-333 |
| 9 | RandomFill 无课程维度口径 | §8.5 P5 补记 | ✅ 行 569-572 |
| 10 | multi 空作答 0 分 + attempt_no | §5.1 / §5 | ✅ 行 248-251 / 230-231 |
| 11 | hint 底线检测口径 + 判题过 execution_slot | §7.1 / §3.2 权衡 14 | ✅ 行 411-413 / 73 |

**计划偏差一并核查**：计划 §3.10 曾裁定「本批预计无需新码」，实际注册了 `4220: 422`（`core/errors.py:9`，coding 作答形态非 `{"source"}`）。属计划预估失误而非实现越权——冒烟实测该码确有消费者且语义正确，spec §8.5 已回写。裁定认可。

## 5. 纪律 grep

| 项 | 结果 |
|---|---|
| 服务层 4 处直连适配器原样未动 | ✅ `git diff 68ef839 -- indexing_service.py model_config_service.py` 为空；`indexing_service.py:30,136`、`model_config_service.py:15-19,223,315` 逐行还在 |
| 新代码零模仿 | ✅ services 全目录仅上述两文件含 `infrastructure.adapters` import |
| `request_id=""` | ✅ 全仓 0 处 |
| models ForeignKey | ✅ 全库 0 处（M-2 现状不破坏） |
| models 全 UTCDateTime | ✅ 非 UTCDateTime 的 DateTime 列 0 处；三张新表的时间列逐列核对 |
| domain 零 infra import | ✅ `app/domain/` 全域 0 处 |
| 端点挂 CurrentRidDep | ✅ 学生 4 + mistake 4 + admin 5 = 13 端点全挂（`main.py:79-81` 全注册） |
| admin CRUD 挂 AdminDep + 审计 | ✅ 五端点全部 `user: AdminDep`；六类 action 实测落库点：`exercise_submit`（service:236）、`exercise_hint`（service:743，finally）、`mistake_reset_mastered`（mistake.py:110）、`admin_exercise_create/update/delete`（service:476/518/554） |

## 6. 三处关键裁定在代码中的落实

| 裁定 | 落实证据 |
|---|---|
| reset_mastered 清零 `consecutive_correct` | `mistake_service.py:196` + 变异 7 击杀 |
| admin PATCH `extra=forbid` 未知字段 422 | `schemas/exercise.py:67`（`ConfigDict(extra="forbid")`，`source` 刻意不在字段表） |
| 前端 AI 参考评分区分 judge_mode | `ExerciseView.vue:187-189`（`mock_heuristic` → 「AI 参考评分（Mock 启发式）」） |

## 7. 内容抽查 + 真服务端到端冒烟（45/45）

**静态**：40 题种子逐项核对 —— 题型 choice 10 / multi 6 / blank 8 / short 8 / coding 8；难度 1×8 / 2×10 / 3×12 / 4×7 / 5×3；12 标签封闭词表每个 ≥3 题（要求 ≥2）。

**真服务**（独立临时库 + `uvicorn --workers 1`，全程 HTTP 不碰测试替身，本任自建脚本实测 **45/45 通过**）：

- 列表 total=40、facets 12 标签、`type=coding`→8、`knowledge_tag=循环`→6、学生详情零泄题（无 answer/explanation/test_cases）、coding 题附 `language`
- 错误作答入错题本；画像含「列表」标签且 wrong_count 降序；推荐 5 条标注封闭、weak_tags 回显、零泄题
- **零错题学生 → 5 条推荐全部 `filled_by=random`**（RandomFill 闭环实证）；`limit=21` → 422
- 掌握度端到端：连对 2 次 mastered=true → 答错 **硬回滚** mastered=false 且 cc=0、wrong_count 保留 → 再次掌握 → 手动重置 **consecutive_correct=0**；他人条目 4040
- coding 可解性（真子进程、经 HTTP）：参考答案 **100 分**、`judge_detail.method=executed`、逐用例四要素齐全（3 cases）、`budget_limit_s=15`；错误答案 0 分；`{"solution":...}` 非 `{source}` 形态 → **4220**；黑名单逐用例 blocked → 0 分
- hint SSE 双意图（Mock 模式）：事件序列、**done 九字段精确齐全**、intent 回显、无知识库时 `degraded=true` + `fallback_reason="no_relevant_chunk"`（降级可见）；`review_my_code` 缺 answer → 422；不存在习题 → 流前 404
- 学生访问 admin CRUD → 4030；`make seed` 退出码 0

**前端两页可演示性**（代码级核对 + 冒烟后端侧实证）：hint 双入口（「获取思路」/「批改我的作答」+ 无作答禁用提示）、`AbortController`、`DegradedBanner`（三值 fallback_reason 各配专属文案 + 兜底文案）、`CitationList`/`MarkdownView` 复用、judge_mode 区分标注、错题本三态筛选/画像/推荐/重置确认对话框/「随机补足」tag（`filled_by === 'random'` 驱动）—— 全部可见。

## 8. 对执行 agent 提请裁定事项的裁定

| 事项 | 裁定 | 理由 |
|---|---|---|
| 偏离 a：hint 中断注册表作用域键取 `user_id:exercise_id`（替代计划的 `(exercise_id, request_id)`） | **认可** | 习题是共享实体，裸 exercise_id 会跨用户掐流；chat 安全是因为会话属单用户。正确的安全决策，已回写 spec §8.1 |
| 偏离 b：`GET /mistakes` 裸数组不分页 | **认可** | 与会话/知识库列表同构；单学生条目上限即题库规模（40），分页无收益 |
| 偏离 c：`mistake_reset_mastered` 写审计 | **认可** | 改变学习状态的用户关键行为留痕，同 `chat_conversation_delete` 先例 |
| 偏离 d：新增 `domain/exercise/shapes.py` | **认可** | admin 写入校验与种子自校验共用一份跨题型规则，防两套口径漂移 |
| 偏离 e：PATCH 显式 null → 422 | **认可** | 入口拒绝优于 NOT NULL 违约变 5000 |
| 偏离 f：`/exercises?focus=` 深链 | **认可** | 错题本→习题跳转闭环所需 |
| 偏离 g：`test_exercise_models` 补 options | **认可** | Task 11 收紧契约后的必要适配，非放宽 |
| 偏离 h：submit 路由 TOCTOU 双查询未改 | **认可，不返工** | 单 worker 演示规模窗口极小；且实测 `stream_hint` 侧的二次取 published 已正确降级为流内 error（冒烟复验过）。改动牵服务层签名，收益不抵风险。登记为已知限制 |
| 不采纳 3：hint/chat finally 审计的 anyio 取消域 | **认可不单开批次** | 与 `chat_service` 逐行同构（P2 已交付并审阅过），属跨批次一致性改造；演示场景下「断连瞬间审计可能丢失」影响极低。**归 P6 后清理批次，若届时触及 chat_service 可顺带**；P5 已用 `aclose()` 专项用例守住正常断连路径 |
| 4220 注册进 CODE_STATUS（计划预估失误） | **认可** | 见 §4 |

## 9. 遗留项归属确认（维持报告 §8 建议，本任逐条核定）

| 条目 | 归属 |
|---|---|
| H-1 负载敏感 flake（勿满载并行跑测试） | 清理批次/CI 引入时处理；口径已在健康检查 + 完成报告 + 本审阅三处固定 |
| hint/chat finally 审计 anyio 取消域 | P6 后清理批次（见 §8） |
| submit 路由 TOCTOU 双查询 | 已知限制，不排期 |
| README 默认密码警示 + 「make seed 后含 40 道习题」说明 | P6（README 收口） |
| admin·exercise 前端管理页 | P6 |
| `PUT /admin/model-config` LLM/档位部分 | P6 |
| lint 存量 35 errors | P6 之后清理批次 |
| 真实 OpenAI 兼容提供方全链路 | 答辩前（同 P3 遗留，需 API Key） |
| 真浏览器 AbortController 下 hint 断连审计落库 | 未验证（agent 已如实标注），不阻塞 |
| 前端两页无 vitest 用例 | agent 自设缺口，spec §10 未要求，接受 |
| 浏览器视觉项（对比度/四断点） | 未验证，与健康检查遗留口径一致 |

## 10. 审阅处置记录

本任审阅过程未修改任何业务代码；变异测试 8+1 处全部字节级还原；验收临时脚本与日志已清理；工作区在审阅结束时为 clean。

**结论：P5 验收通过，可进入合并。等待用户下达合并令。**
