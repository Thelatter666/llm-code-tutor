# README + 收尾批次审阅与裁定（第三任总指挥 · 项目最后一批）

- 审阅日期：2026-09-01
- 审阅对象：`feat/readme-finalize`（计划 42617c3 → 交付 30a1efc → 口径修正 055a3e3）
- 审阅依据：README 批次执行提示词五块范围 + 阶段一裁定（①–⑦）+ 完成报告门槛 V1–V6
- 审阅方式：全文通读 + 事实点独立抽查 + 禁词独立 grep + 交付态基线独立复跑
- **结论：验收通过，可进入合并。发现 1 处口径错误（RandomFill「同课程随机题」），已修正（055a3e3）；1 处 spec 内部矛盾登记为遗留。**

---

## 1. 交付与改动面核实

- 分支 `feat/readme-finalize`，3 次 commit（计划/README/报告）+ 本任口径修正 1 次
- **改动面 `git diff --name-only main...HEAD` = 恰 4 个授权文件**（AGENT.md / README.md / 计划 / 完成报告），业务代码零改动 ✅
- 工作区 clean；README 80 → 264 行

## 2. 独立复跑（交付态基线，V5 复核）

| 门槛 | 报告声称 | 本任独立实测 | 判定 |
|---|---|---|---|
| 后端 pytest | 1006 passed / 2 skipped（227.16s） | **1006 passed / 2 skipped（218.11s）**（干净串行，未触发 H-1） | ✅ |
| make lint | 0 errors | **All checks passed** | ✅ |
| vitest / vue-tsc / build | 8 / 0 / 45.13s | **8 passed / exit 0 / 43.16s** | ✅ |
| V1 禁词 | 0 命中 | 本任独立 `rg "题目\|试题\|草稿"` = **0**；扩展词（账户/账号/教师/分块/成员/空间）= **0** | ✅ |

## 3. 事实点抽查（V3 复核，独立验证）

| README 断言 | 实测 |
|---|---|
| 12 功能点映射表端点/页面 | 与 spec §11、`main.py` 11 routers、`router/index.ts` 14 页逐项对得上（抽查 6 项全中） |
| 防抄袭三档文案「严格（只给思路与伪代码）/引导（默认，允许 ≤10 行片段）/宽松（允许完整实现，需讲解）」 | `ModelConfigView.vue:18-20` **逐字一致** ✅ |
| `Message.blocked_by_policy` 落库 | `models.py:181` 存在 ✅ |
| 文档解析格式 MD/PDF/DOCX/TXT | `parsers.py` pdfplumber/python-docx/直读 ✅ |
| L-2 掩码规则（≤7 位整体 `****`） | `crypto.py` 实测一致 ✅ |
| /health 无前缀 | 正确（演示跑法段已按此写）✅ |
| 两处「无页面入口」如实声明 | `grep` 复核 README 中入口相关表述均为「无页面入口」/接口级口径，无一处写成「有入口」✅ |

## 4. 发现与裁定分级

### 阻塞 / 高危

无。

### 低危（已修复，055a3e3）

**README 功能点 12 RandomFill 口径错误**：原写「数量不足时以**同课程**随机题补足」——复制了 spec §8.5:621 的**旧文**，但 **P5 补记（spec §8.5:630-631）明确「Exercise 无课程维度，随机补足从全部 published 习题选取」**。答辩演示若被追问「同课程从何而来」，与实现不符（习题表无 course_code 列）。
- 处置：已改为「从全部已发布习题中按难度递增补足」，并括注「习题无课程维度，见 spec §8.5 P5 补记」。重跑禁词 grep 与链接核验确认无副作用。

### 遗留登记（不阻塞）

**spec §8.5:621 正文与 P5 补记并存矛盾**：621 行「补足同课程随机题」旧文未随 P5 回写修正（P5 当时只加了补记未改正文）。README 已修、读者看得到补记澄清，故不阻塞；登记为答辩前可选文档打磨项（或后续统一术语清理时顺带）。

## 5. 完成报告可信度

V2 链接核验清单、V3 端点/页面对照表、V6 改动面、未验证项（Mock 形态 ≠ 真实遵循度 / 真实 Key 全链路 / 四断点视觉）——逐条与代码现状相符，报告如实，无自证夸大。两处「端点无页面入口」主动如实登记（含 ModelConfigView 提示语与 UI 出入），非缺陷粉饰。

## 6. 审阅处置记录

本任通读 README 全文，抽查 6 类事实点，独立跑 V1 双 grep 与 V5 全基线；修正 1 处口径（055a3e3）；临时文件已清理；工作区在审阅结束时 clean（除已提交的修正）。

**结论：README + 收尾批次验收通过，可进入合并。此为项目最后一批——合并后全部批次收口，交付态 = lint 0 / 1006+2 / 前端三件套绿 / README 答辩口径完备。等待用户下达合并令。**
