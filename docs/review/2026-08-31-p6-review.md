# P6 审阅与裁定（第三任总指挥）

- 审阅日期：2026-08-31
- 审阅对象：`feat/p6-admin-console`（阶段一计划 6669d9f → 交付 0e0f419 → 验收修复 7708589）
- 审阅依据：P6 执行提示词八项裁定 + 计划 §8 回填 + 健康检查 §6/§7 + 第一任交接 §8 方法论
- 审阅方式：全部独立复跑与实拆，不采信 agent 自证；变异测试「改坏 → 目标用例必须失败 → 字节级还原 → `git status` 干净」
- **结论：验收通过，可进入合并。发现 1 处真实测试缺口（自删禁令被末位守卫掩盖），已补独立用例修复（7708589），变异复验击杀。**

---

## 1. 独立复跑结果

| 门槛 | 报告声称 | 本任独立实测 | 判定 |
|---|---|---|---|
| 后端 pytest | 994 passed / 2 skipped | **994 passed / 2 skipped（198.60s）**，与报告一致 | ✅ |
| 收集数 | — | 996 collected（= 994 + 2） | ✅ |
| 2 条 skip | 语料条件 | `test_retrieval_quality_realistic.py:286/308`，与 P5 相同 | ✅ |
| 前端 vitest / vue-tsc | 8 passed / 零错误 | **8 passed / exit 0 零错误** | ✅ |
| vite build | 通过 | **32.52s 通过**，六个管理页独立 chunk（UsersAdmin/ModelConfig/LogsView/OverviewView/AntiPlagiarismStats/ExerciseAdmin）齐备 | ✅ |
| favicon | L-3 已修 | `frontend/public/favicon.svg` 存在，构建已拷入 `dist/` | ✅ |
| lint | 35 存量不变 | **Found 35 errors**（main.py 随 ping 删除净减） | ✅ |
| 串行 build OOM | 已更正口径 | 本任多次串行构建均无 OOM | ✅ 与 §8 更正一致 |

**H-1 flake 复现**：本任全量第 2 次命中 `test_memory_hog_is_killed_by_memory_layer` 1 failed；隔离复跑 **1 passed（0.22s）**——与健康检查/P5 记录的负载敏感机理完全一致，非 P6 回归。

## 2. 变异测试逐条实拆（V1–V7 + 本任补充 S1/S2）

| # | 变异 | 实拆位置 | 击杀用例（本任实测） | 判定 |
|---|---|---|---|---|
| V1 | 级联连带删 AuditLog | `UserService.delete` | `test_hard_delete_cascades_all_tables_and_keeps_audit`（审计 `login` 行被删，**1 failed**） | ✅ |
| V2 | `admin_user_delete` detail 去级联计数 | 同上 | 同用例（`KeyError: submissions_deleted`，**1 failed**） | ✅ |
| V3 | `update_llm_config` 去 `revision += 1` | `model_config_service.py` | `test_put_updates_fields_bumps_revision_and_audits` + `test_put_hot_reload_takes_effect_without_restart`（**2 failed**）——**独立证实 07f6caa 的热生效用例缺口修复真实有效** | ✅ |
| V4 | PATCH setattr 跳过 status | `UserService.update` | `test_patch_disable_is_soft_delete_keeps_login_blocked`（`active` 未转 `disabled`，**1 failed**） | ✅ |
| V5 | `list_logs` 去 action where | `AuditService.list_logs` | `test_logs_filters_by_action_and_user`（**1 failed**） | ✅ |
| V6 | `mask_api_key` 禁短密钥分支 | `crypto.py` | `test_mask_api_key_short_key_leaks_nothing`（`abc****cdef` 泄露，**1 failed**）——**本任首施变异留出空 if 体致语法错误（IndentationError），已自行修正为合法变异后击杀**；执行 agent 报告的同款教训独立复现 | ✅ |
| V7 | `code_runs` 漏删 | `UserService.delete` | 级联用例（`code_runs_deleted` 归零暴露，**1 failed**） | ✅ |
| S1（本任补充） | **自删禁令失效** | `if user_id == admin_id` → `if False` | **17 条全绿 → 变异存活！** 原因：单 admin 场景下末位守卫先抛 4220，自删分支被掩盖 | ⚠️ 见 §3 中危 P6-GAP |
| S2（本任补充） | 末位守卫失效 | `if row.role == ADMIN` → `if False` | `test_hard_delete_last_admin_guard`（**1 failed**）——末位守卫**独立**被守住 | ✅ |

8 处变异全部字节级还原；验收修复后工作区仅剩新用例 diff。

## 3. 裁定分级

### 阻塞

无。

### 高危（同批处理——已修复）

**P6-GAP：自删禁令未被任何用例独立守住（变异 S1 存活）。**
- 机理：`test_hard_delete_self_rejected_4220` 只有单 admin（`_admin_token`），自删请求先被**末位守卫**拦截（同为 4220），断言通过但守的是另一道防线。实证：仅拆自删守卫 → 该用例仍绿；同时拆两道 → 2 failed；仅拆末位守卫 → 末位用例失败。
- 真实行为**无缺陷**：两道守卫都在代码里，多 admin 场景下自删确被 4220 拦截（本任真服务冒烟 30/30 中含此断言）。缺口在**测试有效性**：自删防线的变异无法被击杀。
- **处置（已修，7708589）**：补 `test_hard_delete_self_rejected_even_with_other_admin` —— 先建第二 active admin 使末位守卫不触发，再删自己断言 4220。重施 S1 变异 → **新用例失败（`assert 0 == 4220`）→ 变异被独立击杀** → 还原 → 全量 995/2。
- 教训：与 P5 的 M5 报告失实、执行 agent 的 V3 缺口同源——**变异测试的价值恰恰在于揪出「用例绿灯但防线空转」**。三道防线已闭环。

### 中低危（归档）

- **L-1**：`test_hard_delete_self_rejected_4220` 的名称与意图仍指向自删禁令，但实际由末位守卫兜底——补用例后不再误伤，原用例保留（其守卫末位路径也有效）。不修。
- **L-2**：完成报告 §7 写「分支 @ 07f6caa」，实际分支 HEAD 为 0e0f419（完成报告自身 commit）——报告书写时点差异，无实质影响。
- **L-3**：`model_config_service.py` 新增 `build_llm` 直连（registry 工厂）——仅用于 `POST /admin/model-config/test` 的「只测首选级」路径（绕过降级链），**未进热路径**；与既有「registry 取配置 + runtime 取运行时」惯例的偏离有设计理由，docstring 已说明。归档。

## 4. 裁定 1–9 落实核对（全部通过）

| 裁定 | 代码/用例证据 |
|---|---|
| 1 自删/自停用/自降权 4220 + 末位 active admin 守卫 | `user_service.py:116-117`（自改 role/status 4220）、`:181-185`（自删 4220 + 末位守卫）；用例 `test_patch_*`、`test_hard_delete_self_rejected*`、`test_hard_delete_last_admin_guard`；冒烟「自删 4220（即便有其他 admin）」「末位 admin 自删 4220」 |
| 2 PUT 字段面 / api_key 缺省=不变更 / 空串 422 / openai 无 key 4220 | `update_llm_config` docstring + 实现；冒烟「空串 422 + 原 key 未丢」「null=不变更（key 仍在、model 更新）」 |
| 3 overview 最小集 | 冒烟 `exercises.published == 40`；spec §6.2 补记 |
| 4 出参含 email、无密码 | 冒烟字段面断言 `hashed_password not in` |
| 5 单事务同步级联 | `UserService.delete` 单事务；级联计数 detail |
| 6 favicon.svg | `frontend/public/favicon.svg` + dist 拷入 |
| 7 /admin/ping 移除 | 冒烟 `GET /admin/ping → 4040`；`main.py` 已删 |
| 8 三页口径文案 | 统计页「仅统计学生（role=user）…是度量口径，不是抄袭检出能力」；日志页「仅可查询、不可修改或删除」 |
| 9 KB.owner_id 悬空例外 | `UserService.delete` docstring 双例外 + `test_hard_delete_keeps_owned_knowledge_base` |

## 5. 纪律 grep

| 项 | 结果 |
|---|---|
| H-3 直连适配器原样 | `indexing_service.py:30,136`、`model_config_service.py:17,18,21,25,27`（违规 import 原样未动；P6 只加方法） |
| 新代码零模仿 | services 全目录仅上述两文件含 `infrastructure.adapters` import（新增的 user/overview/audit 服务均 clean） |
| `build_llm` 直连 | 仅 test 端点（见 §3 L-3） |
| `request_id=""` | 0 处 |
| ForeignKey | 0 处 |
| 非 UTCDateTime | 0 处 |
| 新错误码 | 零新增（user_service 只用 4030/4040/4090/4220/5000，全部已在 CODE_STATUS） |
| 端点守卫 | admin_users 4 端点 + admin_stats 2 端点全部 CurrentRidDep + AdminDep |
| 审计 action | `admin_user_create/update/delete`（user_service:94/143/237）+ `admin_model_config_update`（model_config_service:128） |

## 6. spec 漂移核查

- §6.2「admin 端点补记（P6，管理后台收口）」：users 出参/校验/自操作禁令/末位守卫/KB.owner_id 例外、model-config GET/PUT 字段面与 api_key 语义、test 端点、logs 筛选、overview 最小集、stats 口径、ping 移除说明 —— 全部落档 ✓
- §8.9「P6 实记（2026-08-31）」：单事务级联 + 计数审计 + 自删禁令 + 末位守卫 + KB.owner_id 例外 + docstring 级联清单要求 ✓

## 7. 真服务冒烟（本任独立复跑）

独立临时库 + `uvicorn --workers 1`，全程 HTTP 黑盒，**30/30 通过**：
seed admin 登录、用户列表字段面、创建/4090、软删停用 4030、重启恢复、自删 4220（多 admin 场景）、末位守卫 4220、硬删除七类级联计数（submissions=1、mistake_entries=1 等全对齐）、审计保留（victim 登录审计仍可查）、日志 action 筛选、model-config 掩码（`sk-****7890` 中段不泄露）、revision 自增（3→4）、model 热生效、空串 422 且原 key 未丢、null=不变更、test 端点 mock ok=true、overview `exercises.published == 40`、stats 可查、`/admin/ping → 4040`。

## 8. 执行 agent 报告的两项发现复核

1. **V3 揭示的热生效用例缺口**：属实且修复有效——本任重施 V3 变异，此前失明的 `test_put_hot_reload_takes_effect_without_restart` 如期失败（2 failed）。修复质量认可。
2. **变异执行方式纪律**（改坏后必须先验证改坏了）：本任 V6 首次实拆同样踩中（空 if 体 → IndentationError 全量收集崩），已修正为合法变异并击杀——同款教训，处置正确。

## 9. 遗留确认（维持 P6 后清理批次归属）

lint 35 存量、M-3 response_model、M-4 死配置、M-5 「草稿」改口、H-3 分层违规重构（含 `build_llm` 直连的评估）、混合检索、真实 OpenAI Key 全链路。

## 10. 审阅处置记录

本任审阅共执行 9 处变异（V1–V7 + S1 + S2），全部字节级还原；补 1 条测试用例（7708589）并重施 S1 验证击杀；真服务冒烟 30/30；临时文件已清理。审阅结束时工作区 clean（除已提交的修复）。

**结论：P6 验收通过，可进入合并。等待用户下达合并令。**
