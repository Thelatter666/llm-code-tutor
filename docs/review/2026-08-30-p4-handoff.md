# P4 批次交接文档（在线代码编辑器与受限执行器）

> 交接时间：2026-08-31。交接人：P4 执行 Agent。接收人：下一个执行 Agent。
> 本文档只陈述**已验证事实**，推测一律标注「未验证」。数值证据均为真实测试输出，可复跑。

---

## 一、任务与分支

- 任务书：见对话原文（P4 批次），实施计划见 `docs/superpowers/plans/2026-08-30-p4-editor-sandbox.md`（已 commit，含基准实测表）
- 分支：`feat/p4-editor-sandbox`，基点 `7a2cbf2`，**未 merge、未 push**
- 直接依据：`docs/adr/0003-code-sandbox-resource-limits.md`（**必须先读**）、`docs/adr/0002-single-worker-constraint.md`、spec §3.1 / §3.2 / §5 / §6.2 / §8.3 / §9

## 二、已完成并 commit（7 个提交）

| Commit | 内容 | 测试数 |
|---|---|---|
| `5bd7e68` | P4 实施计划文档 | — |
| `0bd6466` | 领域层执行策略（黑名单扫描 + 受限命令装配，零 IO） | test_execution_policy.py |
| `63a7aee` | CodeSession / CodeRun 表 + limit_detail 结构 | test_code_run_models.py |
| `584957c` | CodeExecutor 端口 + SubprocessCodeExecutor 适配器（四层限制真起进程实测） | test_code_executor.py 63 条 + test_execution_leaks.py 8 条 |
| `407fabb` | CodeService.run（并发上限 2 + 429 + run_in_threadpool 卸载 + 落库 + 审计） | test_code_run_service.py 15 条 |
| `730222c` | POST /code/run + 草稿 CRUD + GET /code/runs 路由 | test_code_run_api.py 21 条 |

**最后一次全量测试（在 `730222c` 上跑）：后端 658 passed / 2 skipped，前端 8 passed。**
此后 Task 6 的前端改动**尚未跑过任何一次全量测试**（被用户手动取消）。

## 三、工作区未提交内容（Task 6，代码已完成但未 commit）

```
 M frontend/package-lock.json      （+ monaco-editor ^0.56.0）
 M frontend/package.json
 M frontend/src/api/code.ts         （runCode / listRuns / 草稿 CRUD）
 M frontend/src/components/AppShell.vue  （+「在线代码编辑器」导航项，icon EditPen）
 M frontend/src/router/index.ts     （+ /editor 路由，() => import(...) 懒加载）
 M frontend/src/types/code.ts       （RunStatus / LimitDetail / CodeRunOut / CodeSessionOut 等）
 M frontend/vite.config.ts          （manualChunks + monaco）
?? frontend/src/components/CodeEditor.vue    （Monaco 包装组件）
?? frontend/src/views/student/CodeEditorView.vue  （编辑器页）
```

**Task 6 已验证：**
- `npx vue-tsc --noEmit` 零错误
- `npx vite build` 成功：2981 modules，2m51s
- 产物（Monaco 确认单独成块、按需加载）：
  - `monaco-Df6frexO.js` 4,002.67 kB（gzip 1,034.43 kB）
  - `ts.worker-DTZAwq0V.js` 7,043.07 kB
  - `CodeEditorView-BiOOahIv.js` 9.81 kB（gzip 4.26 kB）← 路由级懒加载
  - `index-M_mHYPOr.js` 56.21 kB（gzip 21.48 kB）← 业务主包
- 基线对比：P3 报告记业务包约 57 kB，现为 56.21 kB，**无回归**
- `dist/` 目前是**从未提交代码构建**的，验收前端时注意

**Task 6 未完成：**
1. 跑一次 `make test` 确认无回归
2. **浏览器中实际验证编辑器可编辑并运行**（用户明确要求证据，不能只说构建通过）
3. commit（建议消息：`feat(frontend): 在线代码编辑器页，Monaco 路由级懒加载与运行输出面板（spec §6.2 §8.3）`）

## 四、剩余工作（Task 7，未开始）

按计划文档 Task 7 执行，产出 `docs/review/2026-08-30-p4-completion-report.md`，12 条限制层实测逐条给数值证据。

### 已由测试获得的数值证据（可直接引用，来自 pytest -s 输出）

| # | 验证项 | 实测结果 |
|---|---|---|
| 1 | 持续吃内存 | status=memory_exceeded，**peak_rss=292.4MB**，elapsed=0.90s，samples=9，exit_code=-9 |
| 2 | 死循环不占内存 | status=timeout，**exit_code=-24（SIGXCPU）**，elapsed=3.10s |
| 3 | 纯睡眠 10s | **elapsed=5.05s**，exit_code=-9（墙钟杀） |
| 4 | 写大文件 | Python 端 errno=27（EFBIG），JS 端 EFBIG；**落盘实测 1,048,576 字节**（= 上限） |
| 5 | 输出超长 | **stdout 截断到 8192 字节**，实际产出 1,048,581 字节 |
| 6 | 命中黑名单 | rule=os_system，**副作用文件未产生**（确认未执行） |
| 7 | JS 死循环 | status=timeout，exit_code=-24，elapsed=3.13s |
| 8 | 孙进程逃逸 | 孙进程 pid=37087，执行后存活=**False**，耗时 5.04s |

### Task 7 还需补做（起真实 uvicorn 逐条验）

| # | 验证项 | 说明 |
|---|---|---|
| 9 | 事件循环不冻结 | 执行期间高频打 /health ≥50 次，给中位数/P95/最大值，并与空闲基线对比 |
| 10 | 并发第 3 个请求 429 | 走真实 HTTP，非 pytest 内存客户端 |
| 11 | 临时目录无泄漏 | 成功/超时/被杀/异常四路径各验一次（pytest 已覆盖，报告需复述数值） |
| 12 | 无僵尸进程 | 批量执行后 psutil 扫描无 zombie 子进程 |
| — | 前端浏览器实测 | Monaco 可编辑、语言切换、运行按钮、输出面板 |
| — | 降级路径 | 某层在当前平台不支持时 limit_detail 的真实内容 |
| — | 变异测试指引 | 逐条「拆掉某修复后哪条测试会失败」，**必须实际跑过** |

### Task 7 遗留的前置缺口（重要）

1. **仓库目前没有顶层 `README.md`**。ADR-0003 明确要求「黑名单防误触不防攻击 + 被执行代码以当前 OS 用户身份运行」**写入 README 与后台页面**。
   - 页面侧：已在 `CodeEditorView.vue` 加了 `el-alert` 安全边界声明 ✅
   - README 侧：**未做 ❌** —— 需要创建并写入
2. `frontend/docs/ui-baseline.md` 是否被 CodeEditorView 完全遵守，未逐条核对。

## 五、关键实现位置（接手必读）

| 关注点 | 文件 |
|---|---|
| 四层限制实现 | `backend/app/infrastructure/adapters/execution/subprocess_executor.py` |
| 端口定义（Protocol） | `backend/app/infrastructure/ports/code_executor.py` |
| 黑名单 / 状态机 / 阈值常量 | `backend/app/domain/code/execution.py` |
| 并发信号量 + 429 | `backend/app/infrastructure/concurrency.py` 的 `execution_slot()` |
| 服务编排 | `backend/app/services/code_service.py` 的 `run()` |
| Monaco worker 配置 | `frontend/src/components/CodeEditor.vue` |
| limit_detail 结构 | `backend/app/infrastructure/adapters/execution/subprocess_executor.py` 的 `_detail()` |

**绝对禁止：** 在 `subprocess_executor.py` 里出现 `resource.setrlimit(resource.RLIMIT_AS|DATA|RSS)` —— macOS 下会抛 `ValueError: current limit exceeds maximum limit`（ADR-0003 实测），照抄 Linux 写法直接崩。已有测试 `test_code_executor.py::test_implementation_never_sets_rlimit_as_data_or_rss` 守住这条。

## 六、环境与命令

```bash
# 后端测试
cd backend && . .venv/bin/activate && python -m pytest -q
# 全量（含前端 vitest，顺序执行非并行）
make test
# 类型检查 + 构建
cd frontend && npx vue-tsc --noEmit && npx vite build
# 起服务（做 12 条实测用）
make dev      # 双端口 5173 + 8000
make serve    # 单端口 8000，需要先有 frontend/dist
```

- Python 虚拟环境在 `backend/.venv`，依赖已装好（含 psutil）
- `monaco-editor@^0.56.0` 已在 `frontend/package.json`
- 测试若用了 `pytest-randomly` 之类乱序插件，调试时可加 `-p no:randomly`
- AGENT.md 约束：**禁止 git push / remote add**；commit 已预授权按 Task 粒度执行；merge 需单独下令

## 七、待用户拍板的遗留决策（接手后先问清）

1. **测试负载问题**：用户反馈跑测试时电脑卡顿。初步判断是内存压力（每个吃内存用例把子进程推到 292MB，全量跑约 7-8 次；主 pytest 进程经 chromadb 顶层导入拉起重依赖）。已提出但**未执行**的缓解方案：
   - 把「内存打满」用例改为可注入阈值（如 64MB）做行为验证，只保留 1 条用真实 256MB 阈值
   - 减少 `make test` 频率（每 Task 一次，而非每小步一次）
   - `vite build` 只在最终验证跑一次
   - 用 `nice` 降优先级
   - **这些都没做**，因为会偏离「全部用真实阈值实测」的任务书要求，需要用户确认
2. Task 6 之后的 `make test`（含浏览器验证）还没跑，是否需要用户在场配合。

## 八、报告要求（不可省略）

完成报告 `docs/review/2026-08-30-p4-completion-report.md` 必须含 12 节，其中第 3 节「限制层实测表」逐条给数值证据，**任何一条没验到就写「未验证」，禁止用「理论上生效」糊弄**。参考 P1/P2/P3 报告的既有格式（同目录）。
