# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/

# 密钥与环境变量（ADR：API Key 加密主密钥，丢失即无法解密已存密钥）
.env
.env.*
!.env.example
.secret_key

# 运行时数据：SQLite 演示库与 Chroma 本地持久化目录
*.db
*.sqlite
*.sqlite3
data/
storage/

# Node / 前端构建产物
node_modules/
dist/
.vite/
*.local

# 编辑器与系统
.DS_Store
.idea/
.vscode/
*.swp

# 工具产物：由 `uipro init --ai codebuddy` 生成（约 4.8MB），可重现，不入库
.codebuddy/
